/**
 * TypeScript types derived from token schema
 * 
 * These types are re-exported from tokenSchema.ts for convenience
 */

export type {
  TokenSchema,
  BaseTokens,
  SemanticTokens,
  PaletteTokens,
  TypographyTokens,
  SpacingTokens,
  ShapeTokens,
} from './tokenSchema';
